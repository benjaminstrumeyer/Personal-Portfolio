app.controller('aboutMeController', function() {
     var self = this;
    //TODO: Should access a hobby service to put on the scope. 
    // This should be passed through to the hobbyCircleDirective. 
    
});